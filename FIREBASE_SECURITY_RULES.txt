// Copy and paste these rules into Firebase Console > Firestore Database > Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is admin
    function isAdmin() {
      return request.auth != null && 
        request.auth.token.email == 'iamaaritmalhotra@gmail.com';
    }
    
    // Students collection - admin can manage all, students can read their own
    match /students/{studentId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }
    
    // Assignments collection - admin can manage all, students can read their own
    // This checks if the user's email is in studentEmails array OR if they're admin
    match /assignments/{assignmentId} {
      allow read: if request.auth != null && 
        (isAdmin() || 
         (resource.data.studentEmails != null && 
          request.auth.token.email in resource.data.studentEmails) ||
         (resource.data.studentIds != null && 
          request.auth.token.email in resource.data.studentIds));
      allow write: if isAdmin();
    }
    
    // Tests collection - admin can manage all, students can read their own
    match /tests/{testId} {
      allow read: if request.auth != null && 
        (isAdmin() || 
         (resource.data.studentEmails != null && 
          request.auth.token.email in resource.data.studentEmails) ||
         (resource.data.studentIds != null && 
          request.auth.token.email in resource.data.studentIds));
      allow write: if isAdmin();
    }
    
    // Folders collection - admin only
    match /folders/{folderId} {
      allow read, write: if isAdmin();
    }
  }
}

// ============================================
// SIMPLIFIED VERSION (Use this for testing)
// ============================================
// This allows all authenticated users to read/write everything
// Use this temporarily until you update existing assignments/tests with studentEmails

/*
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
*/

